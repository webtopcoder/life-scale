# Replace remaining staging widget URLs with production

## Change
- Replace every remaining staging service reference with `https://sirius.bigdog.app`.
- Cover runtime fallbacks, development deployment defaults, tests, examples, and documentation.
- Leave unrelated API addresses unchanged.

## Validation
- Confirm no staging widget URL remains anywhere in the project.
- Run the focused imported-funnel tests and TypeScript validation.
