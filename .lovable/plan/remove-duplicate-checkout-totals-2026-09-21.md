# Remove duplicate checkout totals

- Remove the redundant “Due today: USD 1.00” row from `/onboarding-rvr2` and `/onboarding-rvr2-tt`.
- Remove the same confirmed duplicate from `/onboarding-888` and `/onboarding-888-tt`.
- Keep each payment widget, surrounding checkout design, and alternate-flow order summary unchanged.
- Verify all four checkout variants show the amount only inside their payment widget.
