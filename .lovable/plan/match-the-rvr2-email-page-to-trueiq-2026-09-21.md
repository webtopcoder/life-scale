# Match the RVR2 email page to TrueIQ

## Current difference
The selected RVR2 screen is not an exact copy. It currently uses the older shared email/results screen. The current TrueIQ RVR2 funnel instead selects its dedicated V2 screen.

## Changes
- Copy the current TrueIQ RVR2 V2 email screen into this project.
- Use it only for `/onboarding-rvr2` and `/onboarding-rvr2-tt` through their shared RVR2 checkout.
- Match its copy, content order, blurred chart, email form, disabled/active button behavior, result tiles, benefit list, timer, viewer count, spacing, and animation.
- Preserve this project's existing session ID and analytics integrations where TrueIQ-specific integrations were intentionally excluded.
- Leave the email screens for IQ, 888, 888-TT, FF, and other funnels unchanged.

## Verification
- Complete enough of both RVR2 variants to open the email overlay at mobile size.
- Compare the rendered screen against the current TrueIQ implementation.
- Confirm a valid email advances to the existing RVR2 checkout without changing its surrounding design or payment widget.
