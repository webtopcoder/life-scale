Adapt the Product Manager language on the `/careers` page to sound simple and friendly, matching the tone of the Media Buyer role.

## Scope
- Only edit `src/pages/CareersPage.tsx`.
- Update the `Product Manager` entry in the `ROLES` constant: its `focus` sentence and the three strings in `owns`.
- Leave the Media Buyer role, role keys, and the rest of the page untouched.

## Proposed new copy
- focus: "Build the parts of Life Scale people use every day and keep coming back for."
- owns:
  1. "Decide what to build next and why it matters to our users."
  2. "Make sure new features ship smoothly and work as promised."
  3. "Turn user feedback and usage data into better releases."

## Verification
- Run the TypeScript check after the edit.
- Confirm the `/careers` page renders both roles correctly.
